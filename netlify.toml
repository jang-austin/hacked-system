[build]
  command = "npm cache clean --force && npm install rollup --ignore-scripts && npm run build"
  publish = "dist"

[build.environment]
  VITE_NODE_ENV = "production"
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
