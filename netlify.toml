[build]
  command = "npm cache clean --force && rm -rf node_modules .vite dist && npm install && npm run build"
  publish = "dist"

[build.environment]
  VITE_NODE_ENV = "production"
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
